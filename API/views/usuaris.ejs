<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuaris</title>
    <link rel="stylesheet" type="text/css" href="./styles.css">
</head>
<body>
    <header>
        <h1>¡Gestión de reservas por <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Llista de Usuaris</h2>
            <ul class="product-list">
                <% data.usuaris.forEach(usuari => { %>
                    <li class="product-item">
                        <strong><%= usuari.nom %></strong> 
                        <span><%= usuari.rol %></span>
                        <span><%= usuari.contrasenya %></span>
                    </li>
                <% }) %>
            </ul>
        </section>
    </main>
</body>
</html>